#include <stdlib.h>
#include <stdio.h>

/**
 * main - To print the numbers btwn 0 & 9
 * followed by newline.
 *
 *
 * Return: Always 0 (success)
 */
int main(void)
{

	for (int i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
